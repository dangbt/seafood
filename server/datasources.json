{
  "seafood_db": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME}",
    "password": "${DB_PASSWORD}",
    "name": "seafood_db",
    "user": "${DB_USERNAME}",
    "connector": "mongodb",
    "useUnifiedTopology": true,
    "useNewUrlParser": true
  }
}
